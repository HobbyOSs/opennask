sudo: required

dist: focal

language: cpp

cache: apt

branches:
  only:
    - master
    - issues/*

notifications:
  email:
    - idiotpanzer@gmail.com

addons:
  apt:
    packages:
      - cmake
      - cmake-data
      - ninja-build
      - cpputest
      - libc6-dev-i386
      - linux-headers-generic
      - bison
      - flex
      - libfl-dev
      - libbison-dev

before_install:

install:

before_script:

script:
  - bash ./rebuid.sh
