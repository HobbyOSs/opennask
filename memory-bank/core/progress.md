# Progress

## 実装済み
- CMake ビルドシステムを外部ツール `gosk` を使用するように変更。
- `projects/cmake_*.sh` スクリプトを `gosk` に対応するように修正。
- `projects/cmake_*.sh` の依存関係を修正 (`asmhead.bin` 生成)。

## まだ必要な実装
- (なし)

## 関連情報 / 既知の問題
- **`gosk` のアセンブル問題:** `gosk` (v0.0.0-20250406052136-a15b20e3eece 時点) が `projects/03_day/harib00i/asmhead.nas` のアセンブルに失敗する（エラーメッセージなし）。複雑な構文（プロテクトモード移行、GDT 設定など）への対応が不完全な可能性。これにより、`03_day` 以降の一部のサンプルプロジェクトのビルドが失敗する。
